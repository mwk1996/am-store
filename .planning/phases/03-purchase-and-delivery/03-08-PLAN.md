---
phase: 03-purchase-and-delivery
plan: 08
type: execute
wave: 5
depends_on: ["03-06", "03-07"]
files_modified:
  - components/purchase/GatewaySelector.tsx
  - components/purchase/KeyRevealBox.tsx
  - components/purchase/OrderStatusBadge.tsx
  - components/purchase/DeliveryTimeline.tsx
  - components/purchase/ConfirmReceiptDialog.tsx
autonomous: true
requirements: [ORDER-01, ORDER-02, ORDER-03, ORDER-04, DELIVERY-01, DELIVERY-04]

must_haves:
  truths:
    - "GatewaySelector renders 5 Iraqi gateway tiles in a 2-3 col grid with selected/hover states"
    - "KeyRevealBox fetches GET /api/orders/[id]/key on button click and displays the decrypted key with copy button"
    - "OrderStatusBadge maps every order status (PENDING, PAID, DELIVERED, COMPLETED, DISPUTED, REFUNDED, FAILED) to a colored badge"
    - "DeliveryTimeline renders a 3-step stepper reflecting MANUAL delivery progress and shows credentials when DELIVERED"
    - "ConfirmReceiptDialog shows a confirmation dialog that POSTs /api/orders/[id]/confirm and calls onConfirmed() on success"
  artifacts:
    - path: "components/purchase/GatewaySelector.tsx"
      provides: "RadioGroup tile grid of 5 Iraqi gateways"
      contains: "GatewaySelector"
    - path: "components/purchase/KeyRevealBox.tsx"
      provides: "Monospace key display with copy button and fetch-on-reveal"
      contains: "KeyRevealBox"
    - path: "components/purchase/OrderStatusBadge.tsx"
      provides: "Status → colored Badge mapping for all 7 order statuses"
      contains: "OrderStatusBadge"
    - path: "components/purchase/DeliveryTimeline.tsx"
      provides: "3-step MANUAL delivery stepper with credentials display"
      contains: "DeliveryTimeline"
    - path: "components/purchase/ConfirmReceiptDialog.tsx"
      provides: "Confirm receipt Dialog with DELIVERED status gate"
      contains: "ConfirmReceiptDialog"
  key_links:
    - from: "components/purchase/KeyRevealBox.tsx"
      to: "/api/orders/[id]/key GET"
      via: "fetch on reveal button click with Authorization header"
      pattern: "api/orders.*key"
    - from: "components/purchase/ConfirmReceiptDialog.tsx"
      to: "/api/orders/[id]/confirm POST"
      via: "fetch on dialog confirm with Authorization header"
      pattern: "api/orders.*confirm"
---

<objective>
Build the 5 shared purchase/delivery UI components that the page layer (Plan 03-10) composes. These are the reusable building blocks for gateway selection, key reveal, status display, delivery tracking, and receipt confirmation.

Purpose: Separating components from pages keeps each file within context budget and allows Plan 03-10 to import and compose these without re-implementing logic.
Output: 5 component files in components/purchase/. All "use client". All i18n strings via useTranslations(). All layouts follow 03-UI-SPEC.md exactly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-purchase-and-delivery/03-UI-SPEC.md
@.planning/phases/03-purchase-and-delivery/03-CONTEXT.md
@.planning/phases/03-purchase-and-delivery/03-PATTERNS.md

<interfaces>
API contracts (from Plans 06 + 07):

GET /api/orders/[id]/key → response: { key: string }
POST /api/orders/[id]/confirm → response: { success: true }

Order status values: "PENDING" | "PAID" | "DELIVERED" | "COMPLETED" | "DISPUTED" | "REFUNDED" | "FAILED"

Available shadcn/ui components: Button, Badge, Dialog, DialogContent, DialogHeader, DialogTitle, RadioGroup, RadioGroupItem, Toast
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared purchase/delivery components (GatewaySelector, KeyRevealBox, OrderStatusBadge, DeliveryTimeline, ConfirmReceiptDialog)</name>
  <files>components/purchase/GatewaySelector.tsx, components/purchase/KeyRevealBox.tsx, components/purchase/OrderStatusBadge.tsx, components/purchase/DeliveryTimeline.tsx, components/purchase/ConfirmReceiptDialog.tsx</files>
  <read_first>
    - .planning/phases/03-purchase-and-delivery/03-UI-SPEC.md (Component Inventory, Screen §1 gateway selector, §3 key reveal box, §4 order list status badge, §5 delivery timeline and confirm receipt — every spec detail must be implemented)
    - app/[locale]/dashboard/page.tsx (STATUS_STYLES or STATUS_COLORS map to reuse the color convention)
    - components/ui/ (list available: Button, Badge, Dialog, DialogContent, DialogHeader, DialogTitle, RadioGroup, RadioGroupItem, Textarea, Toast)
  </read_first>
  <action>
Create the components/purchase/ directory and all 5 component files. All use `"use client"` directive. Follow UI-SPEC.md exactly for colors, spacing, and copy.

**components/purchase/GatewaySelector.tsx:**
- Props: `{ value: string; onChange: (value: string) => void; disabled?: boolean }`
- Uses shadcn/ui `RadioGroup` + `RadioGroupItem`
- Grid: `grid grid-cols-2 sm:grid-cols-3 gap-3`
- 5 gateway tiles: `{ id: "zaincash", label: "ZainCash" }, { id: "qi-card", label: "QI Card" }, { id: "fib", label: "FIB" }, { id: "asia-pay", label: "Asia Pay" }, { id: "fast-pay", label: "Fast Pay" }`
- Each tile: `rounded-xl border border-border/60 bg-secondary/50 p-4 cursor-pointer transition-all min-h-[44px] flex flex-col items-center justify-center gap-1`
- Selected: `border-primary/60 bg-primary/10 ring-1 ring-primary/40`
- Hover: `hover:border-primary/30`
- Label: `text-xs font-semibold text-center`

**components/purchase/OrderStatusBadge.tsx:**
- Props: `{ status: string }`
- STATUS_COLORS map per UI-SPEC.md §4:
  - PENDING: `text-amber-400 bg-amber-500/10`
  - PAID: `text-blue-400 bg-blue-500/10`
  - DELIVERED: `text-purple-400 bg-purple-500/10`
  - COMPLETED: `text-emerald-400 bg-emerald-500/10`
  - DISPUTED: `text-red-400 bg-red-500/10`
  - REFUNDED: `text-gray-400 bg-gray-500/10`
  - FAILED: `text-red-400 bg-red-500/10`
- Renders shadcn/ui Badge with `className={STATUS_COLORS[status] ?? STATUS_COLORS.PENDING}`

**components/purchase/KeyRevealBox.tsx:**
- Props: `{ orderId: string; token: string; initialKey?: string }`
- State: `revealedKey`, `loading`, `copied`
- On mount: if `initialKey` provided, set revealedKey immediately (no fetch needed)
- Reveal button: ghost Button with `Key` lucide icon — `onClick` fetches `GET /api/orders/${orderId}/key` with `Authorization: Bearer ${token}` header
- Key display box: `rounded-lg border border-border/60 bg-secondary/60 px-4 py-3 break-all select-all font-mono text-sm`
- Key box wrapper: `relative` — copy button `absolute end-2 top-2` (logical property for RTL)
- Copy: `navigator.clipboard.writeText(key)`, icon swaps Copy→Check for 2s, toast "Copied!"
- Email notice: `text-xs text-muted-foreground` — "We've also sent this key to your email"
- Card wrapper: `rounded-2xl border border-primary/20 bg-primary/5 backdrop-blur-sm p-6`
- Section label: `text-xs font-semibold uppercase tracking-wider text-muted-foreground mb-3` — "Your License Key"

**components/purchase/DeliveryTimeline.tsx:**
- Props: `{ status: string; deliveredAt?: string | null; confirmedAt?: string | null; credentials?: string | null }`
- 3 steps:
  1. "Order Placed" — always CheckCircle2 text-emerald-400
  2. "Awaiting Delivery" / "Delivered" — CheckCircle2 text-emerald-400 if status in [DELIVERED, COMPLETED]; else Clock text-amber-400
  3. "Receipt Confirmed" — CheckCircle2 text-emerald-400 if COMPLETED; else ghost circle
- Connector: `w-px h-8 bg-border/60 ml-3`
- Each step: `flex items-start gap-3`; icon `h-4 w-4 shrink-0 mt-0.5`
- Credentials box (when status=DELIVERED and credentials present): same key-box style — label "Account Credentials", `font-mono text-xs`
- Copy button on credentials box: same navigator.clipboard pattern

**components/purchase/ConfirmReceiptDialog.tsx:**
- Props: `{ orderId: string; token: string; onConfirmed: () => void; confirmDeadline?: string | null }`
- Trigger: accent Button "Confirm Receipt" — if confirmDeadline provided, show "Auto-confirms in N hours" below the button as `text-xs text-muted-foreground`
- Dialog title: "Confirm Receipt?"
- Dialog body: "By confirming, you verify you've received the account credentials and the seller will be paid."
- Two buttons: "Cancel" (outline) and "Confirm Receipt" (accent, `bg-accent`)
- On confirm: POST `/api/orders/${orderId}/confirm` with `Authorization: Bearer ${token}` → on success: toast "Receipt confirmed. Order completed." + call `onConfirmed()`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "components/purchase" | head -10</automated>
  </verify>
  <done>
    - All 5 component files exist under components/purchase/ with "use client" directive
    - `grep "GatewaySelector" components/purchase/GatewaySelector.tsx` returns the export
    - `grep "/key" components/purchase/KeyRevealBox.tsx` returns the fetch URL
    - `grep "STATUS_COLORS" components/purchase/OrderStatusBadge.tsx` returns the map
    - `grep "DeliveryTimeline" components/purchase/DeliveryTimeline.tsx` returns the export
    - `grep "/confirm" components/purchase/ConfirmReceiptDialog.tsx` returns the POST fetch
    - `npx tsc --noEmit` produces no errors from components/purchase/
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → key reveal component | Auth token sent as Authorization header on every key fetch |
| Browser → confirm receipt dialog | Auth token sent as Authorization header on confirm POST |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08-01 | Information Disclosure | KeyRevealBox — token stored in localStorage | accept | Consistent with existing dashboard auth pattern; httpOnly cookie migration is Phase 4+ |
| T-03-08-02 | Information Disclosure | Key visible before payment confirmed | mitigate | KeyRevealBox only renders when parent page confirms status in [PAID, COMPLETED]; PENDING state handled in Plan 03-10 |
| T-03-08-03 | Tampering | ConfirmReceiptDialog — unauthenticated confirm | mitigate | API returns 401 without token; component always passes stored token |
</threat_model>

<verification>
```bash
npx tsc --noEmit 2>&1 | grep "components/purchase"
grep "GatewaySelector" components/purchase/GatewaySelector.tsx
grep "/key" components/purchase/KeyRevealBox.tsx
grep "STATUS_COLORS" components/purchase/OrderStatusBadge.tsx
grep "/confirm" components/purchase/ConfirmReceiptDialog.tsx
```
All must return matches. TypeScript produces no errors from components/purchase/.
</verification>

<success_criteria>
- 5 component files exist in components/purchase/ (note: not components/checkout/ or components/orders/ — all in purchase/)
- GatewaySelector renders 5 gateway tiles using RadioGroup with correct selected/hover CSS classes
- KeyRevealBox fetches /api/orders/[id]/key with Authorization header; copy button uses logical CSS (end-2) for RTL
- OrderStatusBadge maps all 7 status values including FAILED to correct Tailwind color classes
- DeliveryTimeline shows 3-step progress and credentials box when status=DELIVERED
- ConfirmReceiptDialog POSTs /api/orders/[id]/confirm and calls onConfirmed() on success
- All components use RTL-safe logical CSS properties (ms-*, me-*, end-*, start-*) where directional
- TypeScript compiles cleanly across all 5 files
</success_criteria>

<output>
After completion, create `.planning/phases/03-purchase-and-delivery/03-08-SUMMARY.md`
</output>
