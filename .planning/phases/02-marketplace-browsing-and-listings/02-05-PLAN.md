---
phase: 02-marketplace-browsing-and-listings
plan: 05
type: execute
wave: 3
depends_on:
  - "02-03"
files_modified:
  - app/[locale]/products/[id]/page.tsx
  - components/product-card.tsx
autonomous: true
requirements:
  - LISTING-06
  - SEC-03

must_haves:
  truths:
    - "Product detail page reads title and description from multilingual JSON via getLocalizedText()"
    - "Product detail page shows stock badge with correct three-state logic (>5 / <=5 / 0)"
    - "Product detail page shows deliveryType indicator (Instant / Manual Delivery)"
    - "Product detail page shows seller info row with name, join date, completed orders count"
    - "GET /api/products/[id] response never includes keyValue field"
    - "components/product-card.tsx is retired (no pages import it)"
  artifacts:
    - path: "app/[locale]/products/[id]/page.tsx"
      provides: "Updated detail page with multilingual JSON + stock + deliveryType + seller info"
  key_links:
    - from: "app/[locale]/products/[id]/page.tsx"
      to: "api/products/[id]"
      via: "fetch in useEffect"
      pattern: "fetch.*api/products"
---

<objective>
Update the product detail page to work with the new Phase 2 schema: read title/description as Json via getLocalizedText(), show the correct three-state stock badge, display deliveryType indicator, update seller info section. Also retire the legacy components/product-card.tsx by removing it and updating any pages that still import it.

Purpose: Buyers see accurate product information including stock urgency, delivery type, and seller credibility signals.
Output: Updated product detail page + retirement of legacy card component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-marketplace-browsing-and-listings/02-CONTEXT.md
@.planning/phases/02-marketplace-browsing-and-listings/02-UI-SPEC.md
@.planning/phases/02-marketplace-browsing-and-listings/02-03-SUMMARY.md

<interfaces>
<!-- Updated Product shape from GET /api/products/[id] (after 02-03) -->
interface Product {
  id: string;
  title: unknown;             // Json — { "en": "...", "ar": "..." }
  description: unknown;       // Json
  price: number;
  imageUrl?: string | null;   // single URL (D-02)
  platform?: string | null;
  deliveryType: "INSTANT" | "MANUAL";
  status: "ACTIVE" | "INACTIVE";
  availableKeys: number;      // from _count.keys where isUsed=false
  seller: {
    id: string;
    name: string;
    avatar?: string | null;
    createdAt: string;
    _count: { sellerOrders: number };  // NOTE: verify this field name from productService.getById()
  };
  category?: { name: string } | null;
}

<!-- Stock badge — same logic as ProductCard (copy StockBadge function inline) -->
// >5: emerald "N in stock"
// 1-5: amber "Only N left"
// 0: red "Out of stock"

<!-- Delivery type indicator from UI-SPEC section 3 -->
// INSTANT: <Key className="w-4 h-4 text-blue-400" /> + "Instant Delivery — key revealed immediately after payment"
// MANUAL:  <Package className="w-4 h-4 text-purple-400" /> + "Manual Delivery — seller posts credentials"
// Style: flex items-center gap-2 text-sm text-muted-foreground

<!-- Pages that currently import legacy components/product-card.tsx — from RESEARCH.md -->
// Check: app/[locale]/page.tsx, app/[locale]/admin/products/page.tsx
// These must be updated to either: use marketplace/ProductCard, remove the card, or inline replacement
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update product detail page for new schema fields</name>
  <files>app/[locale]/products/[id]/page.tsx</files>
  <read_first>
    - app/[locale]/products/[id]/page.tsx (full file — understand current Product interface and render)
    - .planning/phases/02-marketplace-browsing-and-listings/02-UI-SPEC.md (section 3: Product Detail Page)
  </read_first>
  <action>
Update app/[locale]/products/[id]/page.tsx:

1. Update Product interface:
   - title: string → title: unknown (Json)
   - description: string → description: unknown (Json)
   - images: string[] → imageUrl: string | null | undefined
   - type: "GAME_ACCOUNT" | "SOFTWARE_KEY" → deliveryType: "INSTANT" | "MANUAL"
   - Add: platform?: string | null
   - Keep: availableKeys, seller (with _count.sellerOrders), category

2. Add import: `import { getLocalizedText } from "@/lib/utils";`

3. Replace all `product.title` plain string renders with `getLocalizedText(product.title, locale, "Product")`
4. Replace all `product.description` plain string renders with `getLocalizedText(product.description, locale, "")`

5. Update image handling:
   - Remove: `product.images[selectedImg]` multi-image pattern
   - Replace with: `const thumb = product.imageUrl ?? "/placeholder-product.png";`
   - Remove: `selectedImg` state and image thumbnail strip (multi-image gallery is deferred per D-02)
   - Single image display only

6. Replace type badge (GAME_ACCOUNT / SOFTWARE_KEY) with:
   - Category badge: `{product.category && <Badge variant="outline" className="border-primary/30 text-primary bg-primary/10 text-xs rounded-full px-2.5 py-1">{product.category.name}</Badge>}`
   - Platform badge: `{product.platform && <Badge variant="outline" className="border-primary/30 text-primary bg-primary/10 text-xs rounded-full px-2.5 py-1">{product.platform}</Badge>}`

7. Add deliveryType indicator section (below rating, above price box):
   ```tsx
   <div className="flex items-center gap-2 text-sm text-muted-foreground">
     {product.deliveryType === "INSTANT"
       ? <><Key className="w-4 h-4 text-blue-400" /> Instant Delivery — key revealed immediately after payment</>
       : <><Package className="w-4 h-4 text-purple-400" /> Manual Delivery — seller posts credentials</>}
   </div>
   ```

8. Replace stock badge in price box:
   Add inline StockBadge:
   ```tsx
   function StockBadge({ count }: { count: number }) {
     if (count === 0) return <Badge className="text-red-400 bg-red-500/10 border-red-500/20">Out of stock</Badge>;
     if (count <= 5) return <Badge className="text-amber-400 bg-amber-500/10 border-amber-500/20">Only {count} left</Badge>;
     return <Badge className="text-emerald-400 bg-emerald-500/10 border-emerald-500/20">{count} in stock</Badge>;
   }
   ```
   Use: `<StockBadge count={product.availableKeys} />`

9. Update seller section:
   - Seller name: keep `product.seller.name`
   - Completed orders: keep `product.seller._count.sellerOrders` (NOTE: verify field name from service — it may be `sellerOrders` or `orders`; check productService.getById include)
   - Add join date display: `Member since {new Date(product.seller.createdAt).getFullYear()}`

10. Image alt text: `alt={getLocalizedText(product.title, locale, "Product")}`

11. Buy button: disabled when `product.availableKeys === 0` (already implemented — verify this still works with new field name)

12. Remove `product.attributes` section entirely (no longer in schema)
  </action>
  <verify>
    <automated>cd "E:/am stor" && npx tsc --noEmit 2>&1 | grep "products/\[id\]" | head -10</automated>
  </verify>
  <done>
    - No TypeScript errors in app/[locale]/products/[id]/page.tsx
    - grep "getLocalizedText" "app/[locale]/products/[id]/page.tsx" returns 2+ lines
    - grep "images\[selectedImg\]\|product\.type\|product\.attributes" "app/[locale]/products/[id]/page.tsx" returns nothing
    - grep "deliveryType\|StockBadge" "app/[locale]/products/[id]/page.tsx" confirms both additions
    - grep "Only.*left\|in stock\|Out of stock" "app/[locale]/products/[id]/page.tsx" confirms stock badge strings
  </done>
</task>

<task type="auto">
  <name>Task 2: Retire legacy product-card.tsx and fix all its import sites</name>
  <files>components/product-card.tsx, app/[locale]/page.tsx</files>
  <read_first>
    - components/product-card.tsx (full file — understand its props and what pages use it)
    - app/[locale]/page.tsx (check if it imports product-card.tsx and how it uses it)
    - app/[locale]/admin/products/page.tsx (check for import — may need update)
  </read_first>
  <action>
Step 1 — Find all imports of the legacy card:
  grep -r "from.*components/product-card\|from.*product-card" app/ --include="*.tsx" --include="*.ts"

Step 2 — For each file that imports components/product-card.tsx:
  - app/[locale]/page.tsx (storefront / product listing): This page likely shows products with the old schema shape. Update it to either:
    a. Import ProductCard from "@/components/marketplace/ProductCard" and pass the new props, OR
    b. Remove product grid from the storefront page if it's not part of Phase 2 scope (the storefront may just show a hero + link to /marketplace)
    Decision: Import the updated marketplace ProductCard. Map old prop shapes to new ones. If the page fetches products via Prisma directly, update the query to use new field names.
  - Any admin pages using it: Update to use marketplace ProductCard or remove product cards from admin pages (admin has its own product table, not cards)

Step 3 — After fixing all import sites, delete components/product-card.tsx:
  rm "E:/am stor/components/product-card.tsx"
  (Or leave it with a deprecation comment if deletion causes build issues — prefer deletion)

Step 4 — Verify no remaining imports:
  grep -r "product-card" app/ components/ --include="*.tsx" --include="*.ts"
  This must return no results.
  </action>
  <verify>
    <automated>cd "E:/am stor" && grep -r "from.*product-card\|from.*components/product-card" app/ components/ --include="*.tsx" --include="*.ts" 2>/dev/null; echo "Exit code: $?"</automated>
  </verify>
  <done>
    - grep for legacy card imports returns nothing (all import sites updated or removed)
    - components/product-card.tsx is either deleted or has a clear "DEPRECATED — use marketplace/ProductCard" comment at top
    - npx tsc --noEmit shows no errors caused by the legacy card removal
    - app/[locale]/page.tsx compiles cleanly with updated or removed ProductCard usage
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Product detail page | GET /api/products/[id] must never expose keyValue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Information Disclosure | GET /api/products/[id] response | mitigate | productService.getById() uses _count only; verify keyValue absent in response |
| T-02-14 | Information Disclosure | Legacy product-card.tsx with old schema | accept | Retiring the component eliminates the risk surface |
</threat_model>

<verification>
- Product detail page uses getLocalizedText for all text renders
- Stock badge shows three states correctly
- deliveryType indicator present
- No images[] array usage (single imageUrl per D-02)
- Legacy components/product-card.tsx has no live import sites
- npx tsc --noEmit exits 0 for all affected files
</verification>

<success_criteria>
Buyers viewing a product detail page see the correct multilingual title and description, accurate stock count with urgency badge, delivery type explanation, platform/category metadata, and seller info. The legacy product card is retired with no import sites remaining.
</success_criteria>

<output>
After completion, create `.planning/phases/02-marketplace-browsing-and-listings/02-05-SUMMARY.md`
</output>
