---
phase: 02-marketplace-browsing-and-listings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/seed.ts
autonomous: true
requirements:
  - LISTING-01
  - LISTING-02
  - LISTING-03
  - LISTING-04
  - LISTING-05
  - LISTING-06
  - LISTING-07
  - SEC-02
  - SEC-03
  - SEC-04
  - SEC-05
  - SEC-06

must_haves:
  truths:
    - "npx prisma db push runs without errors"
    - "npx prisma generate produces a client with Product.title, Product.status, Product.deliveryType, Product.isFeatured, Product.platform, Product.sellerId, ProductKey.keyValue, ProductKey.isUsed"
    - "Category rows exist in DB for Game Accounts, Software Keys, Gift Cards, In-Game Items, Other"
    - "OrderStatus enum contains PENDING, PAID, DELIVERED, COMPLETED, DISPUTED, REFUNDED"
    - "User model has sellerProducts relation"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Complete Phase 2 schema"
      contains: "model Category"
    - path: "prisma/seed.ts"
      provides: "Category seed data"
      contains: "upsert"
  key_links:
    - from: "prisma/schema.prisma"
      to: "Product.sellerId"
      via: "User @relation"
      pattern: "SellerProducts"
    - from: "prisma/schema.prisma"
      to: "ProductKey"
      via: "renamed from LicenseKey"
      pattern: "model ProductKey"
---

<objective>
Replace the legacy schema with the Phase 2 multi-seller schema: rename LicenseKey → ProductKey, rename Product.name → Product.title, add all missing fields (sellerId, deliveryType, status, isFeatured, platform, categoryId), add Category model, expand OrderStatus enum, and seed category rows.

Purpose: Every subsequent plan in Phase 2 depends on the Prisma client reflecting the new schema. This plan is the hard blocker for all other plans.
Output: Updated schema.prisma + seed.ts + generated Prisma client with correct types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-marketplace-browsing-and-listings/02-CONTEXT.md
@.planning/phases/02-marketplace-browsing-and-listings/02-RESEARCH.md

<interfaces>
<!-- Current schema — executor must understand what is being changed -->
Current Product model: name Json, category String?, licenseKeys LicenseKey[], NO sellerId, NO deliveryType, NO status, NO isFeatured, NO platform
Current LicenseKey model: key String @unique — NO isUsed, NO usedAt, NO keyValue
Current OrderStatus enum: pending, paid, failed (lowercase)
Current User model: NO sellerProducts relation

<!-- Target state — implement exactly this -->
Enums to add/update:
  DeliveryType { INSTANT, MANUAL }
  ProductStatus { ACTIVE, INACTIVE }
  OrderStatus { PENDING, PAID, DELIVERED, COMPLETED, DISPUTED, REFUNDED }  ← uppercase, replaces existing lowercase values

Model: Category { id String @id @default(cuid()), name String, slug String @unique, products Product[] }

Model: ProductKey (renamed from LicenseKey) {
  id        String    @id @default(cuid())
  keyValue  String              ← renamed from 'key', remove @unique (duplicates rejected at app layer)
  isUsed    Boolean   @default(false)
  usedAt    DateTime?
  productId String
  product   Product   @relation(fields: [productId], references: [id], onDelete: Cascade)
  orderId   String?   @unique
  order     Order?    @relation(fields: [orderId], references: [id])
  createdAt DateTime  @default(now())
}

Model: Product (updated) {
  id           String        @id @default(cuid())
  title        Json          ← renamed from 'name'
  description  Json          ← already exists, keep
  price        Decimal       @db.Decimal(10, 2)
  imageUrl     String?       ← keep existing field name
  platform     String?       ← NEW per D-05
  deliveryType DeliveryType  @default(INSTANT)  ← NEW per D-03
  status       ProductStatus @default(ACTIVE)   ← NEW per D-06
  isFeatured   Boolean       @default(false)    ← NEW per D-07
  sortOrder    Int           @default(0)        ← keep existing
  sellerId     String        ← NEW per D-08
  seller       User          @relation("SellerProducts", fields: [sellerId], references: [id])
  categoryId   String?       ← NEW (replaces category String?)
  category     Category?     @relation(fields: [categoryId], references: [id])
  createdAt    DateTime      @default(now())
  updatedAt    DateTime      @updatedAt
  keys         ProductKey[]  ← renamed from licenseKeys
  orders       Order[]
  @@index([sellerId])
  @@index([status])
  @@index([isFeatured])
}

User model: add sellerProducts Product[] @relation("SellerProducts")

Order model: update licenseKeyId/licenseKey → productKeyId/productKey referencing ProductKey; update status enum ref to new OrderStatus uppercase values
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite schema.prisma with Phase 2 models and enums</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (full file — understand current state before touching)
    - .planning/phases/02-marketplace-browsing-and-listings/02-RESEARCH.md (Critical Schema Delta section)
  </read_first>
  <action>
Rewrite prisma/schema.prisma completely (preserve generator + datasource blocks). Apply ALL changes:

1. Remove legacy OrderStatus enum { pending, paid, failed }.
   Add: enum OrderStatus { PENDING PAID DELIVERED COMPLETED DISPUTED REFUNDED }

2. Add: enum DeliveryType { INSTANT MANUAL }

3. Add: enum ProductStatus { ACTIVE INACTIVE }

4. Add model Category:
   model Category {
     id       String    @id @default(cuid())
     name     String
     slug     String    @unique
     products Product[]
   }

5. Rename model LicenseKey → model ProductKey. Rename field 'key String @unique' → 'keyValue String' (remove @unique). Add 'isUsed Boolean @default(false)', 'usedAt DateTime?'. Keep id, productId, product, orderId, order, createdAt. Relation on Order: rename licenseKey → productKey, licenseKeyId → productKeyId.

6. Update model Product:
   - Rename field 'name Json' → 'title Json' (comment: // { "en": "...", "ar": "..." })
   - Remove field 'category String? @default("General")'
   - Add: platform String?
   - Add: deliveryType DeliveryType @default(INSTANT)
   - Add: status ProductStatus @default(ACTIVE)
   - Add: isFeatured Boolean @default(false)
   - Add: sellerId String
   - Add: seller User @relation("SellerProducts", fields: [sellerId], references: [id])
   - Add: categoryId String?
   - Add: category Category? @relation(fields: [categoryId], references: [id])
   - Rename relation: licenseKeys LicenseKey[] → keys ProductKey[]
   - Add: @@index([sellerId])
   - Add: @@index([status])
   - Add: @@index([isFeatured])
   - Keep: imageUrl, sortOrder, createdAt, updatedAt, orders

7. Update model Order:
   - Rename field 'licenseKeyId String? @unique' → 'productKeyId String? @unique'
   - Rename relation 'licenseKey LicenseKey?' → 'productKey ProductKey?'
   - Status field already references OrderStatus — enum values are now uppercase; this is a breaking change on existing data. Note: run with --force-reset or handle data migration (dev environment — use db push with reset accepted).

8. Update model User:
   - Add: sellerProducts Product[] @relation("SellerProducts")

9. Keep model AdminUser unchanged.

IMPORTANT: After writing, run:
   npx prisma db push --force-reset
   npx prisma generate
(The --force-reset is acceptable in dev; it drops and recreates all tables. No production data exists yet.)
  </action>
  <verify>
    <automated>cd "E:/am stor" && npx prisma validate</automated>
  </verify>
  <done>
    - npx prisma validate exits 0
    - schema.prisma contains: model ProductKey, model Category, enum DeliveryType, enum ProductStatus, enum OrderStatus with COMPLETED
    - Product model has: title Json, sellerId String, deliveryType DeliveryType, status ProductStatus, isFeatured Boolean, platform String?
    - grep "model LicenseKey" prisma/schema.prisma returns nothing (renamed)
    - grep "name.*Json" prisma/schema.prisma returns nothing (renamed to title)
  </done>
</task>

<task type="auto">
  <name>Task 2: Push schema to DB, generate client, and seed categories</name>
  <files>prisma/seed.ts, package.json</files>
  <read_first>
    - prisma/schema.prisma (just written in Task 1 — confirm it's valid before seeding)
    - package.json (check if "prisma" key with "seed" already exists)
  </read_first>
  <action>
Step A — Push schema and generate client:
  npx prisma db push --force-reset
  npx prisma generate

Step B — Create prisma/seed.ts:
```typescript
import { PrismaClient } from "@prisma/client";
const prisma = new PrismaClient();

async function main() {
  const categories = [
    { name: "Game Accounts", slug: "game-accounts" },
    { name: "Software Keys", slug: "software-keys" },
    { name: "Gift Cards", slug: "gift-cards" },
    { name: "In-Game Items", slug: "in-game-items" },
    { name: "Other", slug: "other" },
  ];
  for (const cat of categories) {
    await prisma.category.upsert({
      where: { slug: cat.slug },
      update: {},
      create: cat,
    });
  }
  console.log("Seeded 5 categories");
}

main()
  .then(() => prisma.$disconnect())
  .catch((e) => { console.error(e); prisma.$disconnect(); process.exit(1); });
```

Step C — Add prisma seed config to package.json under "prisma" key:
  "prisma": { "seed": "ts-node --compiler-options {\"module\":\"CommonJS\"} prisma/seed.ts" }

If ts-node is not available, use: "tsx prisma/seed.ts"
Check with: npx ts-node --version 2>/dev/null || npx tsx --version 2>/dev/null
Use whichever is available.

Step D — Run seed:
  npx prisma db seed
  </action>
  <verify>
    <automated>cd "E:/am stor" && npx prisma db seed 2>&1 | grep -E "Seeded|already|upsert|error" | head -10</automated>
  </verify>
  <done>
    - npx prisma db push exits 0
    - npx prisma generate exits 0
    - npx prisma db seed exits 0 with "Seeded 5 categories" or idempotent upsert message
    - Running: node -e "const {PrismaClient}=require('@prisma/client'); const p=new PrismaClient(); p.category.count().then(n=>{console.log(n); p.$disconnect()})" prints 5
    - TypeScript: no errors importing { ProductStatus, DeliveryType, ProductKey } from "@prisma/client" in a test require
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema → DB | Migration resets dev DB — no production data risk in Phase 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | OrderStatus rename | accept | Dev-only --force-reset; production will use migrate deploy with explicit data migration |
| T-02-02 | Tampering | sellerId FK constraint | mitigate | Prisma FK enforces referential integrity at DB level |
</threat_model>

<verification>
After both tasks complete:
- npx prisma validate exits 0
- npx prisma generate produces no TypeScript errors
- Category table has 5 rows (verified via db seed output)
- ProductKey model exists with keyValue, isUsed, usedAt fields
- OrderStatus enum has COMPLETED value (required by SEC-05 cap check query)
</verification>

<success_criteria>
Schema is the new multi-seller shape. Prisma client is regenerated with correct types. 5 category rows seeded. All subsequent plans can import ProductStatus, DeliveryType, ProductKey from @prisma/client without error.
</success_criteria>

<output>
After completion, create `.planning/phases/02-marketplace-browsing-and-listings/02-01-SUMMARY.md`
</output>
