---
phase: 01-auth-and-user-accounts
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - prisma/schema.prisma
autonomous: true
requirements:
  - AUTH-01

must_haves:
  truths:
    - "Prisma client is regenerated with the new User model fields"
    - "Database schema matches prisma/schema.prisma (emailVerified, verificationToken, resetToken, shopName fields exist in DB)"
    - "TypeScript type User includes the new fields from the generated client"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Source of truth for DB schema"
      contains: "emailVerified"
  key_links:
    - from: "node_modules/@prisma/client"
      to: "prisma/schema.prisma"
      via: "prisma generate"
      pattern: "emailVerified"
---

<objective>
[BLOCKING] Push the Prisma schema changes to the database and regenerate the Prisma client. This plan must complete before Plan 05 (UI) runs, because the UI relies on the generated TypeScript types and the DB must have the new columns.

Purpose: Without this step, all auth service calls touching emailVerified, verificationToken, resetToken, or shopName will fail at runtime.
Output: Updated database schema, regenerated Prisma client with correct TypeScript types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-auth-and-user-accounts/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Push schema to database and regenerate Prisma client</name>
  <read_first>
    - prisma/schema.prisma (verify all 6 new fields are present before pushing)
  </read_first>
  <files>prisma/schema.prisma</files>
  <action>
Verify the schema has the expected fields, then run the two required commands in sequence.

**Pre-flight check** — confirm these fields exist in the User model before pushing:
- `shopName             String?   @unique`
- `emailVerified        Boolean   @default(false)`
- `verificationToken    String?   @unique`
- `verificationTokenExp DateTime?`
- `resetToken           String?   @unique`
- `resetTokenExp        DateTime?`

If any field is missing, STOP and report which field is absent. Do not run db push with an incomplete schema.

**Step 1 — Push schema to database:**
```bash
npx prisma db push
```

Expected output: "Your database is now in sync with your Prisma schema."

If the push fails with a conflict (e.g., existing data violates new constraints), check if `shopName @unique` on a column with existing NULL rows is the issue — it should not be, since NULL values are not considered duplicates in PostgreSQL unique indexes. If another constraint error occurs, report it without attempting to fix data.

**Step 2 — Regenerate Prisma client:**
```bash
npx prisma generate
```

Expected output: "Generated Prisma Client ... to ./node_modules/@prisma/client"

Both commands must succeed before this task is marked done.
  </action>
  <verify>npx prisma validate 2>&1 | tail -5</verify>
  <acceptance_criteria>
    - `npx prisma db push` exits with code 0
    - `npx prisma generate` exits with code 0
    - `npx prisma validate` reports "The schema at prisma/schema.prisma is valid"
    - `grep -r "emailVerified" node_modules/@prisma/client/index.d.ts 2>/dev/null | head -3` returns TypeScript type definition lines containing `emailVerified`
  </acceptance_criteria>
  <done>Database has new columns, Prisma client is regenerated, TypeScript types include all new User fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration script → database | Schema push modifies production DB structure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | prisma db push | accept | db push is idempotent for additive changes; new nullable columns do not corrupt existing rows |
| T-04-02 | Denial of Service | unique constraint on shopName | accept | NULL values do not violate unique constraints in PostgreSQL; existing users without shopName are unaffected |
</threat_model>

<verification>
- `npx prisma validate` passes
- New User fields accessible in TypeScript: `npx tsc --noEmit 2>&1 | grep -i "emailVerified\|verificationToken\|resetToken\|shopName"` — no errors
</verification>

<success_criteria>
- Database schema matches prisma/schema.prisma
- Prisma client TypeScript types include all new User fields
- Existing data is unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-and-user-accounts/01-04-SUMMARY.md`
</output>
